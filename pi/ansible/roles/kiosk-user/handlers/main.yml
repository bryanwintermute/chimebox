---
# roles/kiosk-user/handlers/main.yml

- name: Reload systemd and restart getty@tty1
  ansible.builtin.systemd:
    name: getty@tty1.service
    state: restarted
    daemon_reload: true

- name: Reload sshd
  ansible.builtin.systemd:
    name: ssh.service
    state: reloaded
