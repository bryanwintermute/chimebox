---
# roles/panic-button/handlers/main.yml

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart chimebox-panic-daemon
  ansible.builtin.systemd:
    name: chimebox-panic-daemon.service
    state: restarted
    daemon_reload: true
