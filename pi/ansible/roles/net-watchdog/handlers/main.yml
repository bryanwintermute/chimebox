---
# roles/net-watchdog/handlers/main.yml

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart chimebox-net-watchdog.timer
  ansible.builtin.systemd:
    name: chimebox-net-watchdog.timer
    state: restarted
